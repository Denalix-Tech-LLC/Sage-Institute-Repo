# HIPAA-compliant intake endpoint (BAA in place). The New Patient Intake form
# POSTs its JSON payload here over HTTPS. Never commit the real value.
# Copy this file to .env.local and set the real endpoint URL.
NEXT_PUBLIC_INTAKE_ENDPOINT=https://your-hipaa-compliant-endpoint.example.com/intake
